/* RESET & BASE STYLES */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #050505; /* Deep Black */
    color: #1a1a1a;
    overflow: hidden; /* Prevents scrollbars */
    height: 100vh;
    width: 100vw;
}

/* GAME CONTAINER */
#game-container {
    position: relative;
    width: 100%;
    height: 100%;
}

/* VISUAL LAYER (CANVAS) */
/* This sits behind everything */
#visuals-canvas {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    /* Temporary border to show where canvas is until we add JS drawing */
    /* background: radial-gradient(circle at center, #1a1a1a 0%, #000 70%); */
}

/* UI LAYER */
/* Sits on top of canvas */
#ui-layer {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 10;
    pointer-events: none; /* Allows clicks to pass through to canvas if needed */
}

/* SCENARIO CARD (Bottom Right) */
#scenario-card {
    position: absolute;
    bottom: 5%;
    right: 5%;
    width: 400px;
    max-width: 90vw;
    background-color: #ffffff;
    padding: 2rem;
    border-radius: 20px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.5);
    pointer-events: auto; /* Re-enable clicks */
    animation: floatUp 0.8s ease-out forwards;
}

.scenario-title {
    font-size: 1.8rem;
    font-weight: 600;
    margin-bottom: 1rem;
    letter-spacing: -0.5px;
}

.scenario-text p {
    font-size: 1rem;
    line-height: 1.6;
    color: #333;
}

/* CHOICE NODES (General) */
.choice-node {
    position: absolute;
    background: #dcdcdc; /* Light grey per sketch */
    border: none;
    border-radius: 16px;
    padding: 1.5rem;
    width: 280px;
    text-align: left;
    cursor: pointer;
    pointer-events: auto;
    transition: transform 0.2s ease, background 0.2s ease, box-shadow 0.2s ease;
    box-shadow: 0 4px 15px rgba(0,0,0,0.3);
}

.choice-node:hover {
    transform: scale(1.05);
    background: #ffffff;
    box-shadow: 0 0 20px rgba(255, 255, 255, 0.2);
    z-index: 20;
}

.choice-content .label {
    display: block;
    font-size: 0.85rem;
    color: #666;
    margin-bottom: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.choice-content .description {
    font-size: 1rem;
    font-weight: 500;
    color: #000;
    line-height: 1.3;
}

/* CONNECTOR LINES (Visual decoration pointing to center) */
.connector-line {
    position: absolute;
    border-top: 2px dashed #666;
    width: 40px;
    z-index: -1;
}

/* POSITIONING THE CHOICES (Based on your sketch) */

/* Path 1: Bottom Left */
#choice-1 {
    bottom: 20%;
    left: 10%;
}
#choice-1 .connector-line {
    right: -40px;
    top: 50%;
}

/* Path 2: Top Left */
#choice-2 {
    top: 20%;
    left: 8%;
}
#choice-2 .connector-line {
    right: -40px;
    top: 50%;
    transform: rotate(20deg);
}

/* Path 3: Top Right (LOCKED) */
#choice-3 {
    top: 10%;
    right: 15%;
}
#choice-3 .connector-line {
    left: -40px;
    top: 50%;
    transform: rotate(-20deg);
}

/* Path 4: Mid Right */
#choice-4 {
    top: 45%;
    right: 10%;
}
#choice-4 .connector-line {
    left: -40px;
    top: 50%;
}

/* LOCKED STATE STYLING */
.choice-node.locked {
    background: #b0b0b0;
    cursor: not-allowed;
    opacity: 0.9;
}

.choice-node.locked:hover {
    transform: none; /* No scale effect */
    background: #b0b0b0;
    box-shadow: none;
}

/* LOCK OVERLAY (Tooltip) */
.lock-overlay {
    position: absolute;
    top: 0;
    left: 105%; /* Position to the right of the card */
    width: 220px;
    background: #1e1e1e; /* Dark background */
    color: #fff;
    padding: 1rem;
    border-radius: 12px;
    opacity: 0;
    visibility: hidden;
    transform: translateX(-10px);
    transition: all 0.3s ease;
    box-shadow: 0 10px 25px rgba(0,0,0,0.8);
    pointer-events: none;
    z-index: 30;
}

/* Arrow for the tooltip */
.lock-overlay::after {
    content: "";
    position: absolute;
    top: 20px;
    left: -8px;
    border-width: 8px 8px 8px 0;
    border-style: solid;
    border-color: transparent #1e1e1e transparent transparent;
}

/* Show overlay on hover */
.choice-node.locked:hover .lock-overlay {
    opacity: 1;
    visibility: visible;
    transform: translateX(0);
}

.lock-icon {
    margin-bottom: 0.5rem;
    color: #ff4d4d; /* Red lock icon */
}

.lock-text strong {
    display: block;
    font-size: 0.9rem;
    margin-bottom: 0.25rem;
}

.lock-text p {
    font-size: 0.8rem;
    color: #ccc;
    line-height: 1.4;
}

/* ANIMATIONS */
@keyframes floatUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}
